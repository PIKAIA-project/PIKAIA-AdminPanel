import React from "react";

export const About = () => {
  return <pattern>about</pattern>;
};

export const Loading = () => {
  return <p>loading</p>;
};

export const NotFound = () => {
  return <p>not found</p>;
};
